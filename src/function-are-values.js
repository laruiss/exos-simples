/**
 * Exercice 1
 * Créer et exporter une fonction appelée `add` qui prend 2 paramètres
 * et qui renvoie l'addition des deux.
 * (Si vous pouvez, traitez les cas à la marge,
 * par exemple si les arguments ne sont pas de type Number)
*/

/**
 * Exercice 2
 * Créer et exporter une fonction appelée `multiply` qui prend 2 paramètres
 * et qui renvoie la multiplication des deux.
 * (Si vous pouvez, traitez les cas à la marge,
 * par exemple si les arguments ne sont pas de type Number)
*/

/**
 * Exercice 3
 * Créer et exporter une fonction appelée `compute` qui prend 3 paramètres :
 * une **fonction d'opération** (`add` ou `multiply`),
 * et **deux nombres**, et qui renvoie le résultat de l'invocation de
 * cette fonction d’opération avec les deux nombres.
 * (Si vous pouvez, traitez les cas à la marge,
 * par exemple si les deux derniers arguments ne sont pas de type Number, et si le
 * premier argument n’est pas une fonction)
 */

/**
 * Exercice 4
 * Créer une fonction `getOperation()` avec un paramètre qui sera un opérateur
 * sous forme d'une string d'un seul caractère ('+' ou '*')
 * et qui retournera la fonction correspondant à cet opérateur
 * (`add` ou `multiply`, respectivement).
 */

/**
 * Exercice 5
 * Créer une fonction `evaluate()` avec un paramètre qui est une string comprenant un opérateur sous
 * forme d'une string ('+' ou '*')
 */
