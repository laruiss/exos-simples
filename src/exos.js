/**
 * Écrire une déclaration de fonction appelée noop
 * qui ne prend aucun paramètre et qui renvoie undefined
 * et l’exporter
 */
export function noop () {}

/**
 * Écrire une expression de fonction fléchée dans la variable appelée arrowNoop
 * qui ne prend aucun paramètre et qui renvoie undefined
 */
export const arrowNoop = () => {}

/**
 * Écrire une fonction appelée addOrConcat qui prend deux paramètres et qui renvoie l’addition
 * (ou la concaténation) des deux
 * (bien ajouter le mot-clé 'export' en début de ligne comme au lignes 6 et 12)
 */

/**
 * Écrire une fonction appelée add qui prend deux paramètres et qui renvoie l’addition des deux.
 * (bien ajouter le mot-clé 'export' en début de ligne comme au lignes 6 et 12)
 * Si jamais l’un des deux n’est pas de type Number, lever une erreur en ajoutant l’instruction suivante :
 * throw new Error('Les arguments doivent être des nombres')
 */

/**
 * Écrire une fonction appelée orderBurger qui prend 3 paramètres (que vous nommerez comme bon vous semble) :
 * 1. la cuisson qui doit être une string parmi cette liste :
 *   - 'à point'
 *   - 'saignant'
 *   - 'bleu'
 * 2. avec ou sans cornichon (un booléen)
 * 3. le nombre de tranches de fromage (un nombre parmi 0, 1 et 2)
 * Cette fonction doit renvoyer un objet avec 3 propriétés :
 * cooking, pickle, cheese avec respectivement comme valeurs : le premier paramètre de la fonction, le deuxième puis le troisième
 */
